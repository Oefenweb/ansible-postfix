# test file for postfix
---
- hosts: localhost
  remote_user: root
  roles:
    - ansible-postfix
  vars:
    directory_name0: "{{ lookup('pipe', 'pwd') }}"
    directory_name1: "{{ lookup('env', 'PWD') | dirname }}"
